import 'package:flutter/material.dart';

class PerangkatContent extends StatelessWidget {
  const PerangkatContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Ini adalah Halaman Tambah Perangkat',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}